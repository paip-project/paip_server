# PAIP Server Environment Variables
# Copy to .env and customize

# Path to configuration file
PAIP_CONFIG_PATH=config/paip.yaml

# Path to decrypted secrets (from SOPS)
# PAIP_SECRETS_PATH=config/secrets.decrypted.yaml

# Logging level (DEBUG, INFO, WARNING, ERROR)
PAIP_LOG_LEVEL=INFO
